CD(1)                          Wsh Manual                          CD(1)

NAME
    cd - change current shell directory.

SYNOPSIS
    cd [directory]
    cd -

DESCRIPTION
    cd is a Wsh built-in because an external process cannot change the parent
    shell's working directory. Without arguments it changes to HOME or
    USERPROFILE. cd - switches to OLDPWD.
